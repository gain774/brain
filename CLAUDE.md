# 指示

このリポジトリは共有脳。作業を始める前に必ず `BRAIN.md` を読むこと。

- 目的(BRAIN.md 第1部)は不変。運用ルール(第2部)は改善してよい
- 日次リサーチのセッションでは BRAIN.md 第2部「毎日のルーチン」を順番に実行する
- X APIの認証情報は環境変数 `X_API_KEY` / `X_API_SECRET` にある(スクリプトが自動で使う)
- 記録は日本語。knowledgeは量より質
